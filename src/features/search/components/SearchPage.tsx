import { SearchPageContent } from "@/features/search/components";

export default function SearchPage() {
  return <SearchPageContent />;
}

/*
SEARCH_FILE_NOTE
Syntax su dung:
- React function component + export default.
File nay lam gi:
- Route page cho /search.
Flow chay:
- Router vao page nay -> render SearchPageContent.
*/

